export interface Exercise {
    id: number
    name: string
    series: number
    repetitions: number
    group: string
    demo: string
    thumb: string
    created_at?: string
    updated_at?: string
}
